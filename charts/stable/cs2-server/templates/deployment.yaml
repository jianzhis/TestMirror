apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "cs2-server.fullname" . }}
  labels:
    {{- include "cs2-server.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "cs2-server.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "cs2-server.labels" . | nindent 8 }}
        {{- include "cs2-server.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ include "cs2-server.name" . }}
          image: "{{ .Values.image.imageRegistry }}/{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            {{- range .Values.services }}
            - name: {{ .name }}
              containerPort: {{ .port }}
              protocol: {{ .protocol }}
            {{- end }}
          env:
            {{- if kindIs "array" .Values.env }}
            {{- range .Values.env }}
            - name: "{{ .name }}"
              value: "{{ .value }}"
            {{- end }}
            {{- else if kindIs "map" .Values.env }}
            {{- range $key, $value := .Values.env }}
            - name: "{{ $value.name }}"
              value: "{{ $value.value }}"
            {{- end }}
            {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          {{- if .Values.persistence.enabled }}
          volumeMounts:
            - name: {{ include "cs2-server.name" . }}-data
              mountPath: /app/data
          {{- end }}
      {{- if .Values.persistence.enabled }}
      volumes:
        - name: {{ include "cs2-server.name" . }}-data
          persistentVolumeClaim:
            claimName: {{ include "cs2-server.fullname" . }}-pvc
      {{- end }}