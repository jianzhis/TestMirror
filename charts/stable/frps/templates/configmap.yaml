apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
data:
  frps.toml: |
    [common]
    bindPort = 7000
    token = '12345678'
    dashboard_port = 7500
    dashboard_user = admin
    dashboard_pwd = admin
    vhostHTTPPort = 80
    vhostHTTPSPort = 443