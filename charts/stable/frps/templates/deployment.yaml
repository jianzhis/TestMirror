apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "frps.fullname" . }}
  labels:
    {{- include "frps.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "frps.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "frps.labels" . | nindent 8 }}
        {{- include "frps.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ include "frps.name" . }}
          image: "{{ .Values.image.imageRegistry }}/{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            {{- range .Values.services }}
            - name: {{ .name }}
              containerPort: {{ .port }}
              protocol: {{ .protocol }}
            {{- end }}
          env:
            {{- if kindIs "array" .Values.env }}
            {{- range .Values.env }}
            - name: "{{ .name }}"
              value: "{{ .value }}"
            {{- end }}
            {{- else if kindIs "map" .Values.env }}
            {{- range $key, $value := .Values.env }}
            - name: "{{ $value.name }}"
              value: "{{ $value.value }}"
            {{- end }}
            {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            {{- if .Values.persistence.enabled }}
            - name: {{ include "frps.name" . }}-data
              mountPath: /app/data
            {{- end }}
            - name: frps-config
              mountPath: /etc/frp
      volumes:
        {{- if .Values.persistence.enabled }}
        - name: {{ include "frps.name" . }}-data
          persistentVolumeClaim:
            claimName: {{ include "frps.fullname" . }}-pvc
        {{- end }}
        - name: frps-config
          configMap:
            name: {{ include "frps.fullname" . }}-config