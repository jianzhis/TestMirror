apiVersion: v1
kind: Service
metadata:
 name: {{ include "cataclysm-server.fullname" . }}
 labels:
   {{- include "cataclysm-server.labels" . | nindent 4 }}
spec:
 type: ClusterIP
 ports:
   {{- range .Values.services }}
   - name: {{ .name }}
     port: {{ .port }}
     targetPort: {{ .targetPort }}
     protocol: {{ .protocol }}
   {{- end }}
 selector:
   {{- include "cataclysm-server.selectorLabels" . | nindent 4 }}