apiVersion: v1
kind: Service
metadata:
 name: {{ include "vscode-ssr.fullname" . }}
 labels:
   {{- include "vscode-ssr.labels" . | nindent 4 }}
spec:
 type: ClusterIP
 ports:
   {{- range .Values.services }}
   - name: {{ .name }}
     port: {{ .port }}
     targetPort: {{ .targetPort }}
     protocol: {{ .protocol }}
   {{- end }}
 selector:
   {{- include "vscode-ssr.selectorLabels" . | nindent 4 }}