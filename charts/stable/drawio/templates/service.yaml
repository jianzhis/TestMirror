apiVersion: v1
kind: Service
metadata:
  name: {{ include "niehaohan.fullname" . }}
  labels:
    {{- include "niehaohan.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    {{- range .Values.services }}
    - name: {{ .name }}
      port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ .protocol }}
    {{- end }}
  selector:
    {{- include "niehaohan.selectorLabels" . | nindent 4 }}