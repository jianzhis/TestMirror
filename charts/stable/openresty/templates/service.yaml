apiVersion: v1
kind: Service
metadata:
 name: {{ include "openresty.fullname" . }}
 labels:
   {{- include "openresty.labels" . | nindent 4 }}
spec:
 type: ClusterIP
 ports:
   {{- range .Values.services }}
   - name: {{ .name }}
     port: {{ .port }}
     targetPort: {{ .targetPort }}
     protocol: {{ .protocol }}
   {{- end }}
 selector:
   {{- include "openresty.selectorLabels" . | nindent 4 }}