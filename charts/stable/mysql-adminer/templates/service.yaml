apiVersion: v1
kind: Service
metadata:
  name: {{ include "mysql-adminer.fullname" . }}
  labels:
    {{- include "mysql-adminer.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    {{- range .Values.services }}
    - name: {{ .name }}
      port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ .protocol }}
    {{- end }}
  selector:
    {{- include "mysql-adminer.selectorLabels" . | nindent 4 }}