apiVersion: v1
kind: Service
metadata:
 name: {{ include "left4dead2-server.fullname" . }}
 labels:
   {{- include "left4dead2-server.labels" . | nindent 4 }}
spec:
 type: ClusterIP
 ports:
   {{- range .Values.services }}
   - name: {{ .name }}
     port: {{ .port }}
     targetPort: {{ .targetPort }}
     protocol: {{ .protocol }}
   {{- end }}
 selector:
   {{- include "left4dead2-server.selectorLabels" . | nindent 4 }}