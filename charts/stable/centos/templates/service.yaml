apiVersion: v1
kind: Service
metadata:
 name: {{ include "centos.fullname" . }}
 labels:
   {{- include "centos.labels" . | nindent 4 }}
spec:
 type: ClusterIP
 ports:
   {{- range .Values.services }}
   - name: {{ .name }}
     port: {{ .port }}
     targetPort: {{ .targetPort }}
     protocol: {{ .protocol }}
   {{- end }}
 selector:
   {{- include "centos.selectorLabels" . | nindent 4 }}